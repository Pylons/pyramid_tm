[tox]
envlist =
    py27,py34,py35,py36,pypy,
    py36-pyramid{15,16,17,18,-master},
    docs,
    {py2,py3}-cover,coverage

[testenv]
# Most of these are defaults but if you specify any you can't fall back
# to defaults for others.
basepython =
    py27: python2.7
    py34: python3.4
    py35: python3.5
    py36: python3.6
    pypy: pypy
    py2: python2.7
    py3: python3.4

deps =
    pyramid15: pyramid <= 1.5.99
    pyramid16: pyramid <= 1.6.99
    pyramid17: pyramid <= 1.7.99
    pyramid18: pyramid <= 1.8.99
    master: --editable=git+https://github.com/Pylons/pyramid.git#egg=pyramid
    master: pyramid_retry

commands =
    pip install pyramid_tm[testing]
    nosetests --with-xunit --xunit-file=nosetests-{envname}.xml {posargs:}

[testenv:docs]
basepython = python3.5
whitelist_externals = make
commands =
    pip install pyramid_tm[docs]
    make -C docs html epub BUILDDIR={envdir}

[testenv:py2-cover]
commands =
    pip install pyramid_tm[testing]
    coverage run --source=pyramid_tm {envbindir}/nosetests
    coverage xml -o coverage-py2.xml
setenv =
    COVERAGE_FILE=.coverage.py2
deps =
    --editable=git+https://github.com/Pylons/pyramid.git#egg=pyramid
    pyramid_retry

[testenv:py3-cover]
commands =
    pip install pyramid_tm[testing]
    coverage run --source=pyramid_tm {envbindir}/nosetests
    coverage xml -o coverage-py3.xml
setenv =
    COVERAGE_FILE=.coverage.py3
deps =
    --editable=git+https://github.com/Pylons/pyramid.git#egg=pyramid
    pyramid_retry

[testenv:coverage]
basepython = python3.5
commands =
    coverage erase
    coverage combine
    coverage xml
    coverage report --show-missing --fail-under=100
deps =
    coverage
setenv =
    COVERAGE_FILE=.coverage
